# === OS ===
.DS_Store
Thumbs.db

# === IDE ===
.idea/
.vscode/
*.iml
*.swp
*~

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.env

# === Android / Gradle ===
# NOTE: The Gradle wrapper (gradlew, gradlew.bat, gradle/wrapper/) is intentionally
# committed so build.sh and CI can pin the required Gradle version. Do not ignore it.
mdm-client/.gradle/
mdm-client/build/
mdm-client/app/build/
mdm-client/local.properties
*.apk
*.aab
*.ap_
*.dex

# === Logs ===
*.log

# === Server runtime data ===
mdm-server/device_registry.json
mdm-server/device_registry.json.tmp

# === Misc ===
nanobanana-output/*.png
.claude/settings.local.json
